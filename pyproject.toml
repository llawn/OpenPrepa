[project]
name = "openprepa"
version = "0.1.0"
description = "Cours et corrections pour CPGE"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mkdocs-material>=9.7.6",
    "pymdown-extensions>=10.14",
]
